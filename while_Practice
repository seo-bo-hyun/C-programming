#include <stdio.h>
#include <stdlib.h>
#include <time.h>


int main()
{
	while1();
}

while1()
{
	// 5개의 숫자를 입력받아서 홀수, 짝수를 구분하는 프로그램
	// ============================
	// 짝수, 홀수 구분 프로그램 v1.0
	// ============================

	// 1. num1 -> 6
	// 짝수입니다
	// 2. num2 -> 71
	// 홀수입니다
	// 3. num3 -> 34
	// 짝수입니다
	// 5. num10 -> 23
	// 짝수입니다 

	/*int a, b, c, d, e, f;

	printf("============================\n");
	printf("짝수, 홀수 구분 프로그램 v1.0\n");
	printf("============================\n\n");
	
	

	while (1)
	{

		printf("숫자 입력 : \n");
		scanf("%d", &a);

		if (a % 2 == 0)
		{
			printf("짝수 입니다\n");
			
		}
		if (a % 2 == 1)
		{
			printf("홀수 입니다\n");
			
		}
		
	}*/

	// 2. 1 ~ 100 까지의 숫자중에서 5의 배수만 출력하는 프로그램
	// 조건 1) for, while, do ~ while 사용

	// ==========================
	// 5의 배수 출력 프로그램 v1.0
	// ==========================

	// 5, 10, 15, 20 ~ 95, 100

	//int a = 1;

	/*printf("==========================\n");
	printf("5의 배수 출력 프로그램 v1.0\n");
	printf("==========================\n");

	int a;

	

	for (a = 1; a <= 100; a++)
	{
		if (a % 5 == 0)
		{
			printf("%d\n", a);
		}
	}

	int b = 1;

	while (b <= 100)
	{
		if (b % 5 == 0)
		{
			printf("%d\n", b);
			b++;
		}
	}*/

	// 3. 1 ~ 45의 숫자를 랜덤으로 발생시켜 로또 프로그램 만들기
	// 조건 1) for, while, do ~ while사용
	// 조건 2) 동일한 숫자가 발생하는 것은 무시 (배열에서 처리 필요)

	// ========================
	// 간단한 로또 프로그램 v1.0
	// ========================

	// 23 7 10 11 39 44

	/*printf("========================\n");
	printf("간단한 로또 프로그램 v1.0\n");
	printf("========================\n");

	
	int random = 0;
	srand(time(0));

	for (int a = 1; a <= 6; a++)
	{
		random = rand() & 45;
		printf("%d\n", random);
	}*/

	// 4. 2개의 숫자를 입력 받아서 첫번째 숫자는 시작, 두번째 숫자는 마지막을 나타냄
	// 시작부터 끝까지 수의 합을 구하기
	// 조건 1) 입력받는 2번째의 수가 첫번째 수보다 작거나 같으면 에러를 표시하고
	// 다시 2번째 숫자를 입력 받도록 처리 

	// ======================================
	// 입력 받는 수의 합을 구하는 프로그램 v1.0
	// ======================================

	// 1. num1 입력 -> 1
	// 2. num2 입력 -> 2
	// 1 ~ 10 까지의 합 : 55

	int a, b, c = 0, d;

	printf("======================================\n");
	printf("입력 받는 수의 합을 구하는 프로그램 v1.0\n");
	printf("======================================\n");

	printf("숫자 1 입력 : \n");
	scanf("%d", &a);

	printf("숫자 2 입력 : \n");
	scanf("%d", &b);

	if (a == b)
	{
		printf("오류입니다\n");
	}

	while (a != b)
	{
		c = a + b;

		printf("%d", c);
		break;
	}

	
	
	

		
	


	
	
}
